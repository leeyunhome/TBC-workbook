#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define SEC_PER_HOURS 3600
#define MIN_PER_HOURS 60

int main()
{
	//int hours, minutes, seconds = 0;

	//printf("Input seconds : ______\b\b\b\b\b\b");
	//int flag = scanf("%d", &seconds);

	//while (seconds >= 0)
	//{
	//	//TODO: seconds -> hours, minutes, seconds
	//	minutes = seconds / 60;
	//	seconds = seconds % 60;

	//	hours = minutes / 60;
	//	minutes = minutes % 60;
	//	printf("%d hours, %d minutes, %d seconds\n", hours, minutes, seconds);
	//	printf("Input seconds : ______\b\b\b\b\b\b");
	//	int flag = scanf("%d", &seconds);
	//}


	// by myself
	//hour = second / 3600;
	//minute = (second % 3600) / 60;
	//second = (second % 3600) % 60;


	printf("Good bye");















	// ans
	//int seconds = 0, minutes = 0, hours = 0;

	//printf("Input seconds : ");
	//scanf("%d", &seconds);



	//while (seconds >= 0)
	//{

	//	//minutes = seconds / 60;
	//	//seconds %= 60;

	//	//hours = minutes / 60;
	//	//minutes %= 60;

	//	{
	//		hours = seconds / SEC_PER_HOURS;
	//		minutes = (seconds % SEC_PER_HOURS) / MIN_PER_HOURS;
	//		seconds = seconds % SEC_PER_HOURS % MIN_PER_HOURS;
	//	}
	//
	//	// print result
	//	printf("%d hours, %d minutes, %d seconds\n", hours, minutes, seconds);

	//	printf("Input seconds : ");
	//	scanf("%d", &seconds);
	//}
	//



	//printf("Good bye\n");

	////int div, mod;
	//



	return 0;
}